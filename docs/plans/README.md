# Plans

Local lifecycle packages for planned changes move through `planned/`, `in_progress/`, and `completed/`. Each package contains `SPEC.md`, `PLAN.md`, `TODO.md`, `STATE.md`, and `LEDGER.md`. Evidence and state are governed by `evidence-gate`. These artifacts are local lifecycle records and are not implementation documentation.
